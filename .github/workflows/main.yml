name: CI
on:
  push:
    branches: [github-action, v2]
  pull_request:
    branches: [v2]

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Install packages
        uses: actions/setup-node@v3
        with:
          node-version: '18.x'
      - run: yarn --ignore-scripts
        shell: bash
      - name: Create local node and deploy
        run: |
          yarn hardhat node
      - name: Run Tests
        run: yarn hardhat test